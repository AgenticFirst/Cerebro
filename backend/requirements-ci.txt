# CI-only: excludes heavy voice/ML packages (faster-whisper, kokoro-onnx,
# soundfile) not exercised by the test suite.
# numpy IS required — imported at module level by voice/stt_engine.py and
# voice/tts_engine.py, which are transitively imported from main.py.
fastapi==0.115.12
uvicorn==0.34.2
sqlalchemy==2.0.41
numpy
pytest>=8.0
httpx>=0.27
# File-parsing deps — used by backend/files_parsing/ extractors and exercised
# directly by tests/test_files_parsing.py. Pure Python, no native build, so
# they're cheap to keep in CI and tests fail loudly without them.
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
pypdf>=5.0.0
